import React, {Component} from 'react';

export default class ReviewList extends Component  {
    //constructor for the list of reviews
    constructor(props){
        super(props);
        this.state = {
            //
        }
    }


    render() {
        return (
            {/*review form text box viewers can write in, button to submit a review. Don't need to be able to revise review, but maybe something to add.*/}
            <form onSubmit=
        
        
        )
    }
}